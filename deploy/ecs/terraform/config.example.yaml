# genai-otel-bridge — ECS (DynamoDB-backed HA) default config — GENERATED, do not edit by hand.
#
# Generated from the Go config schema (internal/config/config.go) by `make generate` — the SAME
# source of truth as deploy/helm/values.yaml, so this example can never drift from the schema. The
# drift gate TestECSConfigExampleUpToDate (internal/config) fails CI if it is stale; re-run
# `make generate` and commit. Field comments below are the Go doc-comments.
#
# The ECS Terraform module (deploy/ecs/terraform) injects this verbatim as the GENAI_OTEL_BRIDGE_CONFIG
# container env var; the binary parses it in-memory via config.LoadBytes (no temp file — read-only
# rootfs safe). The ${...} placeholders in the body resolve at load time from the Secrets-Manager-
# injected env vars (the module's secret_arns map).
#
# This is a STARTING POINT, not a drop-in: copy it into your deployment, set ha.dynamodb.table to the
# module's table ("<var.name>-ha"), set ha.dynamodb.region (or rely on the task's AWS_REGION), and
# adjust the source/governance blocks per environment.
emit:
  telemetry:
    otlp:
      # Grafana Cloud OTLP gateway base URL (no trailing /v1/metrics — the emitter appends it).
      endpoint: ${GC_OTLP_ENDPOINT}
      instance_id: ${GC_INSTANCE_ID}
      token: ${GC_OTLP_TOKEN}
      # AllowInsecure opts this endpoint out of the https-only gate for an IN-CLUSTER cleartext OTLP
      # receiver — the regulated/EKS topology where genai-otel-bridge emits to an in-cluster Alloy (which holds the
      # real Grafana Cloud credentials) over the pod network rather than straight to the public gateway.
      # [CP-M7] Default false (https or loopback required). When true, an http NON-loopback endpoint is
      # permitted ONLY IF (a) no credential is set — instance_id/token must be empty, so nothing rides the
      # cleartext link (the collector authenticates to Grafana Cloud, not genai-otel-bridge); and (b) the host is a
      # private target — an IP literal must be RFC-1918/loopback/link-local (a public IP is rejected), while
      # a DNS name (e.g. a Kubernetes Service) is permitted since it can't be resolved at config-load time.
      # https endpoints ignore this flag.
      allow_insecure: false
identity:
  service_namespace: genai-otel-bridge
  # Deployment environment label (e.g. dev, staging, prod).
  deployment_environment: ${ENV}
ha:
  # lease: use a k8s Lease for leader election (production multi-replica). none: disable HA (single-replica dev/test). dynamodb: DynamoDB lock (ECS/AWS).
  coordinator: dynamodb
  # configmap: durable watermark in a k8s ConfigMap (required with coordinator=lease). file: local file (dev only). dynamodb: DynamoDB item (ECS/AWS).
  checkpoint: dynamodb
  # DynamoDB-backed HA (ECS). Required when coordinator|checkpoint == dynamodb. ECS-only ⇒ helm:"omit".
  dynamodb:
    table: genai-otel-bridge-ha
    lock_name: genai-otel-bridge-leader
    lease_duration: 15s
    renew_deadline: 10s
    retry_period: 2s
queue:
  # Per-loop in-memory queue depth (batches). At ~1 batch/min this is ~4h of backlog before the
  # queue blocks the scheduler (the block-on-full backpressure path).
  max_batches: 256
  # Per-batch size cap in bytes (~1 MiB). An over-cap batch is proactively split before transmit;
  # a 413 from the gateway triggers a reactive split (DESIGN §4.5/C11).
  max_batch_bytes: 1048576
  # emit_workers must be 1: per-loop single-flight emit so the watermark advances monotonically to
  # the contiguous successor (DESIGN §4.2/C3; config validates this).
  emit_workers: 1
governance:
  # PerMetricCardinalityBudget caps the number of DISTINCT label-sets tracked per metric NAME — not
  # a global cap. Over-budget series are dropped and counted (genai_otel_bridge_guard_dropped_total). Unset (0)
  # defaults to defaultPerMetricCardinalityBudget. The real ceiling is the downstream Mimir/Adaptive
  # Metrics limit (DESIGN §7 GS2/GS3), not this guard.
  per_metric_cardinality_budget: 10000
  # MaxDPM hard-caps every exported series at ≤ N data points per minute of SERIES (sample-timestamp)
  # time, on BOTH planes. Product plane: emit.CoalesceDPM collapses each (series, 60s) group LWW.
  # Self plane: the PeriodicReader interval is clamped to ≥ 60s/MaxDPM. Unset (0) ⇒ defaultMaxDPM (1);
  # negative is rejected. (followup.md §0.)
  max_dpm: 1
  # MaxCatchupPerTick bounds how many windows a backlogged loop drains per cadence period: when a loop
  # is more than one window behind (only possible when max_backfill > window, e.g. raised per GS2), the
  # scheduler accelerates the tick interval to drain up to N windows, then takes a cadence breather —
  # bounded and fair, and won't burst the source (each call is still rate-limited). Unset (0) ⇒
  # defaultMaxCatchupPerTick (1) ⇒ one window per cadence, i.e. no acceleration (the v1 behaviour);
  # negative is rejected. (DESIGN §7a Cdx-C13/F44.)
  max_catchup_per_tick: 1
  # MaxStreamLabelKeys caps how many OTLP resource attributes a single LOGS loop may contribute to a
  # Loki stream: genai-otel-bridge's product identity resource attrs (service.name/service.namespace/
  # deployment.environment.name) PLUS the loop's indexed attrs (its base content-free set ∪
  # settings.extra_indexed_fields). Loki's max_label_names_per_series (Grafana Cloud default 15) REJECTS
  # — silently drops — a stream over the limit, so the composition root fails fast at startup if a loop
  # would exceed it. Unset (0) ⇒ DefaultMaxStreamLabelKeys (15). Raise ONLY to match a Grafana-staff
  # tenant override of max_label_names_per_series. In the in-cluster-Alloy topology, Alloy's k8s.*/cloud.*
  # enrichment attrs share this same budget — leave headroom.
  max_stream_label_keys: 15
  # AllowLabelKeys are EXTRA content-free attribute keys the operator opts into the cardinality guard's
  # indexed/label allow-list, ON TOP OF the keys each enabled source already declares (so a default
  # deployment needs nothing here). Use it to promote a content-free log attribute to the indexed tier
  # (OTLP resource attr → Loki stream-label candidate). Default empty.

  # LIMITATION (label promotion): listing a key here only allows it PAST the guard. A promoted
  # attribute becomes a QUERYABLE Loki stream label only if it is in the Grafana OTel gateway's default
  # label configuration; any label NOT in that default needs a Grafana support ticket before
  # `{label="…"}` matches — until promoted stack-side it lands as structured metadata, not a stream
  # label. Content-floor keys (message bodies / injected PII) are REJECTED here at startup.
  allow_label_keys: []
log:
  # Log output format: logfmt (default, key=value) or json.
  format: logfmt
  # Minimum level emitted: debug, info (default), warn, or error. Info keeps warnings and errors
  # visible on kubectl logs out of the box; raise to debug at bring-up for per-tick detail.
  level: info
selfobs:
  profiling:
    enabled: false
    mode: pull
    pull:
      addr: :6060
  tracing:
    enabled: false
sources:
  - type: portkey
    enabled: true
    # Source API base URL (public control plane). MUST be https unless http.allow_private is set.
    base_url: https://api.portkey.ai/v1
    # source_instance is part of the CheckpointKey — it namespaces the watermark. Use a stable,
    # env-scoped identifier; changing it resets the watermark (a new bootstrap).
    source_instance: portkey-${ENV}
    auth:
      header: x-portkey-api-key
      value: ${PORTKEY_API_KEY}
    rate_limit:
      # Sustained request rate (req/s). Portkey's shared budget is ~10 req/10s across tenants — stay well under (DESIGN §15).
      rps: 1
      burst: 3
    loops:
      analytics:
        enabled: true
        # Poll cadence (± 10% jitter applied by the scheduler).
        cadence: 60s
        # Query window; 50m keeps bucket granularity at 1 minute (≤55m clamp; H5/OP5c).
        window: 50m
        # bucket_settle: age at which a source bucket stops changing after first observation. 10m is the
        # live-measured default (3m was insufficient); tune up if late-arrival lag is observed in prod.
        bucket_settle: 10m
        # On first run (or after a watermark reset), how far back to bootstrap. Must be ≤ max_backfill.
        bootstrap_lookback: 50m
        # Maximum backfill window. Capped by the metrics backend's accept window — on Grafana Cloud the
        # Mimir out_of_order_time_window is 2h (per-tenant; raise via GS2). 90m leaves margin for clock
        # skew + the catch-up walk; samples older than now-max_backfill are abandoned loud
        # (backfill_unstorable_total). NOTE: unrelated to the ≤55m Portkey granularity clamp.
        max_backfill: 90m
        metric_prefix: portkey_api
        # Configured graph set. An unknown graph is logged and skipped; an all-unknown response is an error
        # (no silent-healthy advance without data).
        graphs:
          - requests
          - cost
          - tokens
          - latency
          - errors

# >>> BEGIN generated source examples — do not edit by hand; run `make generate` <<<
# Example source(s) — NOT active. Copy under `config.sources`, set the secret(s), and
# adjust per environment. Generated from each source package's ExampleSource().
# sources:
#   - type: portkey
#     enabled: true
#     base_url: https://api.portkey.ai/v1
#     source_instance: portkey-<ENV>
#     auth:
#       header: x-portkey-api-key
#       value: <PORTKEY_API_KEY>
#     rate_limit:
#       rps: 1
#       burst: 3
#     loops:
#       groups:
#         enabled: true
#         cadence: 1m
#         settings:
#           # analytics/groups FILTER (optional): scope every graph/group to these Portkey api-key UUIDs (csv), exactly like the notebook's api_key_ids filter. Empty ⇒ workspace-wide (all keys). Set the SAME value on analytics+groups so the per-key aggregates and the per-model/metadata breakdowns agree. NOTE the UUID is the api-key's id (from GET /api-keys), NOT the key secret — and a read-only poller key has no inference traffic of its own, so point this at the key(s) your application actually calls Portkey with.
#           api_key_ids: ""
#           # groups: emit the per-dimension cost gauge in USD (÷100 from Portkey's cents); ON by default
#           emit_cost: "true"
#           # groups: per-prompt request dimension (adds requests_by_prompt{prompt}); default ON, set false to opt out. Prompt slug is a content-free identifier, not content.
#           emit_prompts: "true"
#           # analytics/groups SAFETY (optional): assert the API key's analytics scope is EXACTLY this workspace (slug, e.g. ws-acme-001) before emitting — catches a too-broad/global key (which Portkey can't request-scope on analytics). Mismatch ⇒ refuse to emit + alert; empty ⇒ no check. Set the same value on analytics+groups.
#           expected_workspace: ""
#           # groups: per-poll cap on distinct dimension rows (cardinality guard)
#           max_groups: "10000"
#           # groups: opt-in per-metadata-field group endpoints (csv); each adds a _by_metadata gauge
#           metadata_keys: use_case
#           # page size per fetch (groups: dimension rows; logs_export: records per export page, server ceiling 50000)
#           page_size: "1000"
#           # exclude the still-mutating recent tail from the query upper bound
#           settle: 10m
#           # groups: trailing query window [now-window_span, now-settle]
#           window_span: 1h
#       logs_export:
#         enabled: true
#         cadence: 1m
#         settings:
#           # logs_export: per-Collect emit chunk (bounds memory; the export file is never buffered whole)
#           chunk_max_records: "5000"
#           # logs_export: signed-URL object GET timeout (S3 downloads are larger than control-plane calls)
#           download_timeout: 5m
#           # logs_export: promote content-free fields to the INDEXED (Loki stream-label) tier (csv, default empty); auto-allow-listed in the guard; creates Loki streams (keep LOW cardinality — the per-metric budget is the backstop); needs GS1 stack-side promotion to be queryable as {label=…}; bodies hard-denied
#           extra_indexed_fields: ""
#           # logs_export: opt content-free fields into the structured-metadata allow-list (csv); bodies hard-denied
#           extra_record_fields: cache_status
#           # logs_export: abandon a stuck job after this interval (cancel + restart the window)
#           job_poll_timeout: 30m
#           # logs_export: watermark older than now-max_backfill skipped LOUD. Cap ≤ Loki reject_old_samples_max_age (GC default 7d). Default 24h.
#           max_backfill: 24h
#           # logs_export: tripwire — a window exceeding this pages advances-past with a counted gap (never stalls)
#           max_pages_per_window: "50"
#           # logs_export: lift named sub-keys OUT of the (otherwise hard-denied) Portkey metadata blob into the structured-metadata tier (csv, default empty) — e.g. correlation_id. Only operator-named content-free sub-keys are extracted; the rest of metadata (PII) stays dropped. Bodies hard-denied.
#           metadata_record_fields: ""
#           # logs_export: name ONE metadata sub-key whose UUID value is also mapped to the OTLP log trace_id (logs↔traces correlation), e.g. correlation_id (default empty). Auto-lifted into the record tier too. Bodies hard-denied.
#           metadata_trace_id_field: ""
#           # page size per fetch (groups: dimension rows; logs_export: records per export page, server ceiling 50000)
#           page_size: "50000"
#           # logs_export: fields requested of Portkey export (NOT an egress filter — the client-side strip governs egress)
#           requested_data: ai_model,ai_org,cost,created_at,id,prompt_id,prompt_slug,prompt_version_id,req_units,res_units,response_status_code,response_time,total_units,trace_id
#           # exclude the still-mutating recent tail from the query upper bound
#           settle: 10m
#           # logs_export: REQUIRED — allow-list of S3 hosts for the signed download URL (SSRF guard)
#           signed_url_allow_hosts: signed-url-host.example.com
#           # logs_export: name ONE TOP-LEVEL export field (e.g. Portkey's native trace_id) whose UUID value is mapped to the OTLP log trace_id (logs↔traces correlation), default empty. The alternative to metadata_trace_id_field for deployments that stamp the trace id as a first-class field; the two are mutually exclusive. Auto-added to the record tier + requested_data. A non-UUID value leaves trace_id unset (counted via trace_id_unparsed) but still ships as a record attr.
#           trace_id_field: ""
#           # logs_export: per-export query span; the scheduler is snapshot-gated (LoopConfig.Window==0)
#           window: 1h
#           # logs_export: REQUIRED — Portkey workspace id
#           workspace_id: <your-workspace-id>
#     api_key_use_cases:
#       - label: Data Gen
#         api_key_ids:
#           - <api-key-uuid>
#   - type: langsmith
#     enabled: true
#     base_url: https://api.smith.langchain.com
#     source_instance: langsmith-<ENV>
#     auth:
#       header: x-api-key
#       value: <LANGSMITH_API_KEY>
#     rate_limit:
#       rps: 10
#       burst: 20
#     loops:
#       runs:
#         enabled: true
#         cadence: 1m
#         settings:
#           # runs: promote content-free fields to the INDEXED (Loki stream-label) tier (csv, default empty); auto-allow-listed in the guard; creates Loki streams (keep LOW cardinality — the per-metric budget is the backstop); needs GS1 stack-side promotion to be queryable as {label=…}; message bodies are hard-denied.
#           extra_indexed_fields: ""
#           # opt content-free fields into the structured-metadata allow-list (csv); message bodies are hard-denied.
#           extra_record_fields: tags,child_run_ids,app_path
#           # runs: watermark older than now-max_backfill is skipped LOUD (counted gap). Cap ≤ Loki reject_old_samples_max_age (Grafana Cloud default 7d). Default 24h.
#           max_backfill: 24h
#           # runs: drain cap per window; a window exceeding this advances-past with a counted gap (never stalls)
#           max_pages_per_window: "100"
#           # runs: per-page decode cap (default 32MiB); an oversize page is a loud counted truncation.
#           max_response_bytes: "33554432"
#           # cap on discovered/used sessions (projects) per poll (loud truncation when hit).
#           max_sessions: "1000"
#           # runs: page size for runs/query (server ceiling 100)
#           page_size: "100"
#           # runs: when true, only emit root runs (one log per trace); reduces volume for trace-level correlation
#           root_only: "false"
#           # runs: optional single run_type filter (e.g. chain, llm, tool); empty = all types
#           run_type: chain
#           # filter-bounded session auto-discovery via GET /sessions (alternative to session_ids for the runs loop; also scopes the sessions loop).
#           session_filter: eq(name, "my-project")
#           # runs: static scope (csv of project UUIDs). EITHER this OR session_filter is REQUIRED — never firehose all projects.
#           session_ids: <project-uuid-1>,<project-uuid-2>
#           # runs: auto-discovery session cache TTL (re-queries /sessions after this interval)
#           session_refresh: 5m
#           # runs: exclude the still-running/late tail — runs within settle of now may still be mutating
#           settle: 10m
#           # runs: trailing query span pulled per poll (Window==0 ⇒ snapshot-scheduled)
#           window: 1h
#       sessions:
#         enabled: true
#         cadence: 1m
#         metric_prefix: langsmith
#         settings:
#           # sessions: emit numeric feedback_stats gauges (score averages by feedback key)
#           emit_feedback: "true"
#           # sessions: optional allow-list of feedback keys to emit (csv, content-free operational names); empty = all numeric keys
#           feedback_keys: correctness,helpfulness
#           # cap on discovered/used sessions (projects) per poll (loud truncation when hit).
#           max_sessions: "1000"
#           # sessions + usage: page size for the /sessions list endpoint (offset/limit pagination)
#           page_limit: "100"
#           # filter-bounded session auto-discovery via GET /sessions (alternative to session_ids for the runs loop; also scopes the sessions loop).
#           session_filter: eq(name, "my-project")
#           # sessions + usage: per-session label value — "id" (default, bounded) or "name" (human-readable but potentially high-cardinality)
#           session_label_value: id
#           # sessions + usage: rolling aggregate window [now-stats_window, now] (snapshot; not time-bucketed). For usage, defaults to the cadence so windows tile (sum_over_time ≈ period total).
#           stats_window: 1h
#           # sessions: use approximate (cheaper) backend stats computation
#           use_approx_stats: "false"
#       usage:
#         enabled: true
#         cadence: 10m
#         metric_prefix: langsmith
#         settings:
#           # usage: also emit per-project SPAN counts (langsmith_usage_spans, the storage driver) via one runs/stats call per project per poll; default true. Set false to emit only trace counts (zero extra calls). Bound the fan-out with session_filter/max_sessions.
#           emit_span_counts: "true"
#           # cap on discovered/used sessions (projects) per poll (loud truncation when hit).
#           max_sessions: "1000"
#           # sessions + usage: page size for the /sessions list endpoint (offset/limit pagination)
#           page_limit: "100"
#           # filter-bounded session auto-discovery via GET /sessions (alternative to session_ids for the runs loop; also scopes the sessions loop).
#           session_filter: eq(name, "my-project")
#           # sessions + usage: per-session label value — "id" (default, bounded) or "name" (human-readable but potentially high-cardinality)
#           session_label_value: id
#           # sessions + usage: rolling aggregate window [now-stats_window, now] (snapshot; not time-bucketed). For usage, defaults to the cadence so windows tile (sum_over_time ≈ period total).
#           stats_window: 10m
# >>> END generated source examples <<<
