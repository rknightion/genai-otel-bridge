apiVersion: v2
name: genai-otel-bridge
description: AI Platform Observability Integrator — polls AI platform APIs (Portkey/LangSmith) and pushes OTLP metrics to Grafana Cloud.
type: application
# PodDisruptionBudget policy/v1 is the floor (stable since 1.21). Validated with Helm 4.x.
kubeVersion: ">=1.21.0-0"
version: 0.5.0
# appVersion tracks the released app version (surfaces as app.kubernetes.io/version). Auto-stamped by
# `make changelog VERSION=vX.Y.Z` so it never drifts; bump by hand only if releasing without that step.
appVersion: "3.0.0"
