apiVersion: v1
kind: Service
metadata:
  name: genai-otel-bridge
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "genai-otel-bridge.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "genai-otel-bridge.selectorLabels" . | nindent 4 }}
  ports:
    - name: health
      port: 8080
      targetPort: health
      protocol: TCP
{{- if and .Values.profiling.enabled (eq .Values.profiling.mode "pull") }}
    # pprof endpoint for an Alloy/k8s-monitoring scrape (pull mode).
    - name: pprof
      port: {{ .Values.profiling.pullPort }}
      targetPort: pprof
      protocol: TCP
{{- end }}
