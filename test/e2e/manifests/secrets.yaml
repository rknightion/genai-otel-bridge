apiVersion: v1
kind: Secret
metadata:
  name: aip-oi-secrets
  namespace: aip-oi-e2e
stringData:
  # The e2e config.yaml carries no ${ENV} refs, but the chart's Deployment references this Secret via
  # envFrom unconditionally — so it must exist. The value is an unused dummy.
  PLACEHOLDER: "e2e"
